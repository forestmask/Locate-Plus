org.gradle.jvmargs=-Xmx640m -XX:MaxMetaspaceSize=256m -XX:+UseSerialGC
org.gradle.parallel=false
org.gradle.daemon=false

# Fabric / Minecraft
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.15.11
fabric_version=0.92.2+1.20.1

# Mod
mod_version=1.0.0
maven_group=dev.locateplus
archives_base_name=locate-plus

# Loom: skip the sources remap, which is IDE-only convenience and the peak-memory step
fabric.loom.dontRemapSources=true
